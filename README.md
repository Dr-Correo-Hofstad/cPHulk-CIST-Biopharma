# cPHulk-CIST-Biopharma
While ModSecurity monitors layers 7 application-specific attacks (like HTTP POST vectors, SQL injection, and XSS), cPHulk operates at the authentication layer. It tracks failed authentication attempts for services like cPanel, WHM, SSH, FTP, and IMAP/POP3.
